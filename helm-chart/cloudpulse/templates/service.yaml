apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cloudpulse
spec:
  type: {{ .Values.service.type }}
  selector:
    app: cloudpulse
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
